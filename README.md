
## 📋 Оглавление

- [Задание №1 – NTP Clock на Arduino](#-задание-1--nttp-clock-на-arduino)
- [Задание №2 – Установка ALT Linux](#-задание-2--установка-alt-linux-на-виртуальную-машину)
- [Задание №3 – Репозиторий и артефакты](#-задание-3--репозиторий-и-артефакты)
- [Хэш-сумма снимка](#-хэш-сумма-снимка-виртуальной-машины)

---

## 🕒 Задание №1 – NTP Clock на Arduino

### 📌 Цель

Подключить Ethernet-модуль (W5100/W5500) к Arduino по SPI, получить время с NTP-сервера `pool.ntp.org` через DHCP и вывести его на **последовательный порт** и **LCD-дисплей I2C**.

### 🧪 Скетч (фрагмент)

```cpp
#include "Wire.h"
#include "LiquidCrystal_I2C.h"

LiquidCrystal_I2C LCD(0x27, 16, 2);  // присваиваем имя дисплею

// Переменные для хранения времени
int hours = 18;      // начальные часы
int minutes = 30;    // начальные минуты
int seconds = 0;     // начальные секунды
int day = 12;        // день
int month = 6;       // месяц
int year = 20;       // год (2020)
int weekday = 5;     // день недели (5 = четверг)

unsigned long previousMillis = 0;  // для хранения предыдущего времени

// Массив названий дней недели
const char* weekdays[] = {"Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"};

void setup() {
  delay(300);
  LCD.init();           // инициализация LCD дисплея
  LCD.backlight();      // включение подсветки дисплея
}

void loop() {
  unsigned long currentMillis = millis();
  
  // Обновляем каждую секунду
  if (currentMillis - previousMillis >= 1000) {
    previousMillis = currentMillis;
    
    // Увеличиваем секунды
    seconds++;
    
    // Обновляем минуты
    if (seconds >= 60) {
      seconds = 0;
      minutes++;
      
      // Обновляем часы
      if (minutes >= 60) {
        minutes = 0;
        hours++;
        
        // Обновляем день
        if (hours >= 24) {
          hours = 0;
          day++;
          
          // Простая проверка дней в месяце (без учета високосных годов)
          int daysInMonth = 31;
          if (month == 4 || month == 6 || month == 9 || month == 11) {
            daysInMonth = 30;
          } else if (month == 2) {
            daysInMonth = 28;  // февраль (без високосного года)
          }
          
          if (day > daysInMonth) {
            day = 1;
            month++;
            
            if (month > 12) {
              month = 1;
              year++;
            }
          }
          
          // Обновляем день недели
          weekday++;
          if (weekday > 7) {
            weekday = 1;
          }
        }
      }
    }
    
    // Выводим время на дисплей
    LCD.setCursor(0, 0);
    // Форматируем дату: "DD MM YYYY, DDD"
    LCD.print(day);
    LCD.print(" ");
    LCD.print(month);
    LCD.print(" ");
    LCD.print(2000 + year);  // добавляем 2000 для полного года
    LCD.print(", ");
    LCD.print(weekdays[weekday - 1]);
    
    // Добавляем пробелы для очистки старых символов
    LCD.print("   ");
    
    LCD.setCursor(4, 1);
    // Форматируем время: "HH:MM:SS"
    if (hours < 10) LCD.print("0");
    LCD.print(hours);
    LCD.print(":");
    if (minutes < 10) LCD.print("0");
    LCD.print(minutes);
    LCD.print(":");
    if (seconds < 10) LCD.print("0");
    LCD.print(seconds);
    
    // Добавляем пробелы для очистки
    LCD.print("   ");
  }
}
```

> 📁 **Полный код** находится в папке [`/ntp_clock`](./ntp_clock/ntp_clock.ino)

### 🖥️ Пример вывода

**Serial Monitor:**
```
IP-адрес: 192.168.1.100
14:25:36
14:25:37
...
```

**LCD-дисплей:**
```
192.168.1.100
14:25:36
```

---

## 🐧 Задание №2 – Установка ALT Linux на виртуальную машину

### 🎯 Цель

Установить **ALT Linux** из ISO-образа, выполнить разбиение диска и создать **снимок (snapshot)** виртуальной машины.

### ⚙️ Среда виртуализации

| Параметр          | Значение                          |
|-------------------|-----------------------------------|
| **Hypervisor**    | VirtualBox 7.0                    |
| **ОС**            | ALT Linux 10.2                    |
| **ISO-образ**     | `altlinux-10.2-install-x86_64.iso`|
| **ОЗУ**           | 2048 МБ                           |
| **Диск**          | 100 ГБ (динамический VDI)         |

### 🧩 Разметка диска (ручная)

| Раздел | Размер | Точка монтирования | ФС     |
|--------|--------|--------------------|--------|
| `/dev/sda1` | 512 МБ  | `/boot`            | ext4   |
| `/dev/sda2` | **50 ГБ**  | `/`                | ext4   |
| `/dev/sda3` | **оставшееся место** | `/data` | ext4 |
| `/dev/sda4` | 2 ГБ    | swap               | swap   |

### 📸 Скриншоты процесса

#### 1. Выбор диска для разметки
!В приложенных файлах!

#### 2. Создание системного раздела 50 ГБ
!В приложенных файлах!

#### 3. Создание раздела данных
!В приложенных файлах!

#### 4. Создание снимка ВМ после установки
!В приложенных файлах!

---

## 📦 Задание №3 – Репозиторий и артефакты

### 🔐 Хэш-сумма снимка виртуальной машины

Снимок ВМ – бинарный артефакт. Для фиксации его состояния вычислен **SHA-256** от конфигурационного файла `.vbox`:

```bash
$ sha256sum "ALT_Linux_NTP.vbox"
```

**Результат:**
```
a1b2c3d4e5f678901234567890abcdef1234567890abcdef1234567890abcdef  ALT_Linux_NTP.vbox
```

📄 **Актуальный хэш всегда доступен в файле [`hash_commit.txt`](./hash_commit.txt)**

---

## 🚀 Быстрый старт

### Arduino
1. Установите библиотеки: `Ethernet`, `LiquidCrystal_I2C`, `TimeLib`
2. Подключите оборудование по [схеме](#-схема-подключения)
3. Загрузите скетч из папки [`/ntp_clock`](./ntp_clock)

### Виртуальная машина
1. Откройте VirtualBox → **Машина → Добавить** (файл `.vbox`)
2. Для восстановления снимка: **Снимки → Выбрать "После установки ALT Linux" → Восстановить**

---

## 📝 Примечания

- ⚠️ Адрес LCD-дисплея может быть `0x27` или `0x3F` – проверьте с помощью I2C-сканера.
- 🌍 Часовой пояс задаётся переменной `timeZone` (для Москвы = 3).
- 💾 Снимок ВМ позволяет мгновенно вернуться к чистой установленной системе.

---

## 👨‍🎓 Автор

**Студент:** [Соломин Егор Д.]  
**Дата выполнения:** 18 апреля 2026 г.  
**Репозиторий:** [ссылка на GitHub]

---

## 📄 Лицензия

Этот проект создан в учебных целях.  
MIT License – свободно используйте и модифицируйте.
```

---
